# Backend

I've set up a .env.example file to see the structure of the environment variables used

Run `npm install` and `npm run dev` to install the dependencies and start the server

An ./uploads directory will be created automatically for storing the CSV files
